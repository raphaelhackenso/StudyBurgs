import React from "react";
import { NodeContainerRenderContext, NodeContainerRenderProps, NodeLineRenderProps, NodeLineRenderContext } from "./OrgChart";
interface AnimatedProps<T> {
    node: T;
    props: NodeContainerRenderProps<T> | NodeLineRenderProps<T>;
    context: NodeContainerRenderContext<T> | NodeLineRenderContext<T>;
    duration?: number;
    getStyle?: (node: T, props: NodeContainerRenderProps<T> | NodeLineRenderProps<T>, context: NodeContainerRenderContext<T> | NodeLineRenderContext<T>) => React.CSSProperties;
}
interface AnimatedState {
    entered: boolean;
}
export default class Animated<T> extends React.Component<AnimatedProps<T>, AnimatedState> {
    state: {
        entered: boolean;
    };
    render(): JSX.Element;
}
export {};
