import Node from "./Node";
export default class Step {
    readonly Node: Node;
    readonly X: number;
    readonly Top: number;
    readonly Bottom: number;
    constructor(node: Node, x: number, top: number, bottom: number);
    ChangeTop(newTop: number): Step;
    ChangeBottom(newBottom: number): Step;
    ChangeOwner(newNode: Node, newX: number): Step;
    ChangeX(newX: number): Step;
}
